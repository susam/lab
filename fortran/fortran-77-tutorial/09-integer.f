      program reals

      integer a
      integer b

      a = 2**31 - 1
      b = 2**32 - 1

      write (*,*) a
      write (*,*) a + 1
      write (*,*) b
      write (*,*) b + 1

      stop
      end
